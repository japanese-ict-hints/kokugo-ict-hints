# CLAUDE.md — 国語ICT活用ヒント集

リポジトリ直下に置く作業指示書。セッション開始時に必ず全文を読む。
§2 のハード制約は他のすべての指示に優先する。緩和が必要な場合は実行前に運営者に確認する。

---

## 1. このプロジェクト

小中学校の**国語**の授業で使えるICT活用事例を、**全国の公的機関・事業者サイトから自動収集**し、
教員が30秒で探せる形で並べる静的サイト。

- 利用者：佐倉市の教職員。ICTが得意でない層が主対象
- 端末：iPad・スマホ・校務PC。校内回線は速くない前提
- 運営：個人運営の非公式サイト（教育委員会の公式サイトではない）
- 当面の対象教科：**国語のみ**。他教科は出来栄えを見てから追加

**やらないこと**：教材データの配布／ログイン機能／児童生徒の写真掲載／
外部サイトの写真の転載／記事の全文再掲（§2.1 の条件を満たす場合を除く）

**すでにある資産**：`content-japanese-seed.md`（手作業で作った国語10件）。
フェーズ1ではこれを投入する。うち6件は `verified: listed` で本文未確認なので、収集
パイプラインができたら本文を取得して要約を作り直す。

---

## 2. ハード制約

### 2.1 著作権 — 収集元ごとにライセンスを判定する

このサイトは公開サイトなので、著作権法35条（授業の過程における複製）は適用されない。
一律のルールでは扱えないので、**収集元ごとに `license` を判定して記録する**。

| license | 掲載してよいもの |
|---|---|
| `gov-open` | 本文の複製・翻案が可。出典と、加工した旨の記載が必要 |
| `link-only` | リンク＋自作要約のみ。本文の引用は原則しない |
| `permitted` | 個別許諾の範囲内。許諾内容を `sources.yml` に記録 |

**判定手順（`scripts/license-check.ts`）**

1. 対象ドメインの「利用規約」「著作権について」「このサイトについて」に相当するページを探す
2. 本文に次のいずれかがあれば `gov-open` 候補：
   - 政府標準利用規約／公共データ利用規約
   - CC BY（クリエイティブ・コモンズ 表示 4.0）との互換性の明記
   - 出典明示を条件に複製・公衆送信・翻案を認める記述
3. 「無断転載を禁じます」等があれば `link-only`
4. **判定できなければ必ず `link-only`。** 迷ったら link-only
5. 判定の根拠URLと確認日を `sources.yml` に必ず記録する

確認済みの例：文部科学省ウェブサイト利用規約（最終改正 令和8年8月17日）は、mext.go.jp
等のコンテンツについて出典記載を条件に複製・公衆送信・翻案を認め、CC BY 4.0 と互換である
と定めている（https://www.mext.go.jp/b_menu/1351168.htm 、確認日 2026-09-05）。
→ mext.go.jp は `gov-open`。

**すべての license に共通する禁止事項**
- 外部サイトの**画像・写真・図版を一切配信しない**（ホットリンクも不可）。
  `gov-open` のサイトでも、協力校が提供した授業写真は第三者の権利が残っている
- 児童生徒・教職員が特定できる情報を載せない
- 有料・ログインの先にあるコンテンツを扱わない
- 佐倉市の内部規程の本文を載せない（非公式サイトのため）。「所属校で最新版を確認」と案内する

### 2.2 クロールのマナー

- `robots.txt` を取得し Disallow を尊重する。**404 は「robots.txt が存在しない」＝制限なし**として
  扱う（RFC 9309）。止めるのは、DNS 失敗・接続失敗・5xx・タイムアウトなど**取得に失敗した**場合
  （www.mext.go.jp は 404 を返す。2026-09-06 確認）
- 同一ホストへは **1リクエスト / 2秒以上**の間隔。並列アクセスしない
- User-Agent に連絡先を含める：`SakuraKokugoBot/1.0 (+https://<site>/about)`
- 403 / 429 / 5xx が出たら、そのソースを自動で `paused` にしてログに残す
- 1回の実行で1ホストから取得するページ数の上限は **30**
- 取得した本文は `.cache/`（gitignore）にのみ置き、リポジトリにも公開物にも含めない

### 2.3 URLを推測しない

**訪問していないURLを書かない。** ディレクトリ構造から類推したURL、記憶で書いたURL、
末尾の数字を変えたURLを `sources.yml` にも記事にも入れてはいけない。
必ず一覧ページから辿って、実在を確認したものだけを登録する。
到達できなかったものは `sources.yml` にコメントで理由を残す。

### 2.4 非公式である旨の明示

全ページのフッターと `/about` に表示する：

> このサイトは有志が個人で運営しています。佐倉市教育委員会の公式サイトではなく、
> 掲載内容は市の公式見解ではありません。各ツールの利用可否や生成AIの取扱いは、
> 必ず所属校で最新の規程を確認してください。

---

## 3. 収集源 — 全国

### 3.1 三つの層

| 層 | 対象 | 想定 license | 優先度 |
|---|---|---|---|
| S1 | 国・独立行政法人（文科省、国立教育政策研究所、NISE、教職員支援機構） | `gov-open` の可能性が高い | 高 |
| S2 | **47都道府県＋20政令市の教育センター・教育研修センター・教育委員会** | サイトごとに判定 | 高 |
| S3 | 民間事業者・商業メディア（ベネッセ、Canva、Padlet、Microsoft、EdTechZine 等） | ほぼ `link-only` | 中 |

**S2が本命。** 各都道府県の教育センターは、教科別の指導資料・研究紀要・授業実践事例を
PDFやHTMLで公開しており、国語の単元レベルの事例はここに最も多くある。
特に**中学校国語**は S1・S3 では手薄で、S2 でしか埋まらない見込み。

### 3.2 収集源の発見手順（`scripts/discover.ts`）

以下のリンク集を起点に S2 の実在URLを集める。**いずれも実在を確認済み（2026-09-05）**。

| 起点 | URL |
|---|---|
| 全国教育研究所連盟（教育研究機関リンク集） | https://schit.net/zenkyou/ |
| 全国都道府県教育委員会連合会 リンク | http://www.kyoi-ren.gr.jp/link/ |
| 文科省 都道府県・政令市教育委員会リンク | https://www.mext.go.jp/b_menu/link/1294885.htm |
| 国立特別支援教育総合研究所 センター一覧 | https://www.nise.go.jp/nc/about_nise/link/center |
| 文科省 教育データ標準（教育委員会コード一覧） | https://www.mext.go.jp/a_menu/other/data_00001.htm |

手順：

1. 各リンク集を取得し、リンク先のホスト名を抽出する
2. ホストごとに §2.1 のライセンス判定を実行する
3. そのホスト内で、事例・指導資料の入口になるページを探す
   （「実践事例」「指導資料」「研究紀要」「ICT活用」「授業改善」等を含むページ）
4. RSS/Atom があれば記録する。なければインデックスページのURLを記録する
5. `sources.yml` に追記する。**発見できなかった県はコメントで残す**（後で手作業で探すため）

初回は都道府県から始め、政令市は後回しでよい。1回の実行で扱うのは10ホストまで。

### 3.3 sources.yml の形

```yaml
sources:
  - id: mext-studx
    name: StuDX Style（文部科学省）
    host: www.mext.go.jp
    entry: https://www.mext.go.jp/studxstyle/index2.html
    type: index
    license: gov-open
    license_basis: https://www.mext.go.jp/b_menu/1351168.htm
    license_checked: 2026-09-05
    group: A            # 週次ローテーションの群（§5.2）
    status: active      # active / paused / retired

  - id: benesse-miraiseed
    name: ミライシード ファンサイト
    host: bso.benesse.ne.jp
    entry: https://bso.benesse.ne.jp/miraiseed/fansite/usecase/index.html
    type: index
    license: link-only
    license_basis: 判定不能のため既定値
    license_checked: 2026-09-05
    group: C
    status: active

denylist:
  - reason: 会員登録が必要
    hosts: []
  - reason: 無断転載禁止の明記あり
    hosts: []
```

---

## 4. コンテンツモデル

事例1件 = `src/content/tips/{slug}.md`。Astro の Content Collections + Zod で検証する。

```yaml
---
title: "オンラインの書き込みの乱れを、話し言葉と書き言葉の学習につなげる"  # 40字以内、動詞で終える
summary: "一言日記をオンラインで続けるうちに…"        # 120字以内、Claudeが書く
source:
  url: "https://www.mext.go.jp/studxstyle/skillup/3.html"
  publisher: "StuDX Style（文部科学省）"
  region: "国"                    # 国 / 都道府県名 / 政令市名 / 民間
  published: null                 # 不明なら null
  retrieved: 2026-09-05
  license: gov-open
  verified: fetched               # fetched=本文取得済み / listed=一覧のみ
tools: [teams, miraiseed]
school: [elementary, junior]      # elementary / junior
subjects: [japanese]
domains: [speak-listen]           # §4.2
scenes: [share, reflect]          # §4.2
effort: 1                         # 1=5分以内 2=事前準備30分 3=単元設計が必要
grade: null                       # 明記があれば "小5" 等。推測しない
curriculum: []                    # 学習指導要領コード。フェーズ4
status: draft                     # draft / published / archived
added: 2026-09-05
note: ""                          # 原典が教科や学年を特定していない等の但し書き
---
```

### 4.2 タグ語彙（固定。勝手に増やさない）

**domains**（国語の領域）：`speak-listen`（話すこと・聞くこと） `write`（書くこと）
`read`（読むこと） `language`（知識及び技能・言葉の特徴や使い方） `handwriting`（書写）
`reading-life`（読書）

**scenes**（授業の場面）：`intro` `individual` `share` `discuss` `reflect` `assess` `home` `admin`

**tools**：`okulink-plus`（オクリンクプラス） `drillpark`（ドリルパーク）
`miraiseed`（製品を特定できないとき、または製品をまたぐ資料のときだけ）
`canva` `kahoot` `padlet` `teams` `word` `excel` `powerpoint` `forms` `onenote` `ai` `other`

**扱わないツール**：Google Workspace for Education、ロイロノート・スクール。
佐倉市で採用していないため、語彙に入れない。これらだけを使う事例は収集しない。
（採用状況が変われば見直す）

**subjects**：`japanese` のみ（他教科は語彙だけ先に定義しておき、事例が1件以上ある教科だけを
UIに表示する。`social` `math` `science` `life` `music` `art` `craft` `home` `pe` `english`
`moral` `period` `special` `tech` `general`）

> 設計意図：教員は「今どの場面で困っているか」から探す。scenes と effort を検索の第一級の軸、
> domains を国語特有の絞り込み軸、school を最初の分岐として扱う。

### 4.3 URL設計

```
/                        トップ（検索が主役）
/tips/{slug}/            事例詳細
/scenes/{scene}/         場面別
/domains/{domain}/       領域別
/tools/{tool}/           ツール別
/school/{school}/        校種別
/regions/                収集源の一覧（どこの県の事例が何件あるか）
/updates/  /about/  /privacy/
```

---

## 5. 収集パイプライン

### 5.1 処理順（`scripts/collect.ts`）

1. `sources.yml` から今週の対象群を選ぶ（§5.2）
2. `status: active` かつ `license_checked` が180日以内のソースだけを処理する。
   古いものは §2.1 の判定をやり直す
3. `robots.txt` を確認 → 不許可なら skip してログ
4. RSS があればそれを、なければ `entry` から1階層だけ辿る
5. 既知URL（`.cache/seen.json`）はスキップ
6. 本文を取得 → `.cache/` に保存
7. **スコープ判定**：国語かつ小中学校の事例か。該当しなければ捨てる
   （`seen.json` には記録して二度と取得しない）
8. **重複判定**：URL正規化＋タイトルの類似度。同じ実践が複数サイトに載っていることがある。
   重複なら、より上位の層（S1 > S2 > S3）のものを残す
9. **要約生成**（§5.3）
10. `status: draft` で `src/content/tips/` に書き出す
11. ブランチ `collect/YYYY-MM-DD` にコミットし Pull Request を作成する

**自動マージはしない。** 運営者が要約と著作権上の安全性を確認して `status: published`
に書き換えてからマージする。ここが唯一の品質ゲートなので省略しない。

### 5.2 週次ローテーション

全国のソースを毎週全部回すと負荷も実行時間も過大になる。`group` を A〜D に振り、
毎週1群ずつ処理する（1ソースあたり月1回)。ただし S1 と、更新頻度の高いソースは
毎週の群（A）に入れる。

### 5.3 要約は Claude が書く

**運営者は要約を書かない。** Claude Code の実行時、または `collect.ts` から
Anthropic API を呼んで生成する。次のプロンプトを使う。

```
以下は、学校のICT活用事例を紹介するWebページの本文です。
小中学校の国語の授業でこれを使いたい教員に向けて、以下を出力してください。

- title: 40字以内。何をするのかが分かる動詞で終える見出し。原文の見出しをそのまま使わない
- summary: 120字以内。何をする実践か、どんな場面で効くか、準備の重さが分かるように書く
- domains / scenes / tools / school / effort: 指定の語彙から選ぶ
- note: 原文が教科・学年・ツールを特定していない場合、その旨

制約：
- 原文の語順や言い回しを再利用しない。要約であって抜粋ではない
- 本文に書かれていないことを補わない。学年やツール名が不明なら null にする
- effort は本文からの推定でよいが、推定である旨を note に書く
- 国語の授業に転用できない内容なら、reject: true を返す
```

出力はJSONで受け取り、Zodで検証してから書き出す。検証に落ちたものは PR に含めず、
ログに残す。

### 5.4 リンク切れ検査（`scripts/linkcheck.ts`）

週次で全 `source.url` に HEAD を投げ、404/410 を検出したら Issue を1本にまとめて起票する。
公的機関のPDFはURLが変わりやすいので、これは省略しない。

---

## 6. 技術構成

- **Astro**（最新安定版）。Content Collections + Zod
- **検索**：Pagefind（ビルド時に静的インデックス生成）。絞り込みは素のJS。UIフレームワークなし
- **CSS**：素のCSS + カスタムプロパティ。Tailwind等は使わない
- **ホスティング**：GitHub Pages
- **CI**：GitHub Actions

```
/
├── CLAUDE.md
├── src/
│   ├── content/tips/
│   ├── layouts/  components/  pages/
│   └── styles/tokens.css
├── scripts/
│   ├── discover.ts       収集源の発見（§3.2）
│   ├── license-check.ts  ライセンス判定（§2.1）
│   ├── collect.ts        収集（§5.1）
│   ├── linkcheck.ts
│   └── sources.yml
├── .cache/               取得本文・seen.json（gitignore）
└── .github/workflows/
    ├── weekly.yml        毎週月曜 06:00 JST
    └── deploy.yml
```

---

## 7. UI

### 7.1 考え方

主役は検索と絞り込み。トップページの見栄えではない。「開いた瞬間に探し始められる」ことに
予算を使い、装飾は削る。

### 7.2 トークン（`src/styles/tokens.css`）

桜色を基調にする。白より少し赤みのある紙地に、主色のピンクをリンク・ボタン・見出しラベル・
現在地の表示に使う。**すべての組み合わせで前景と背景のコントラストは 4.5:1 以上**（§8）。
配色を変えるときは、必ず数値で検証してから入れる。

```css
:root {
  --paper:      #FFFAFC;
  --paper-sunk: #FBEEF3;
  --ink:        #241A1E;
  --ink-mute:   #6B5860;
  --rule:       #F0DDE5;

  --accent:      #B3245C;   /* 主色 */
  --accent-hover:#8C1746;
  --accent-soft: #FDEFF4;   /* チップ・入力欄の面 */
  --link:       var(--accent);
  --link-hover: var(--accent-hover);

  /* 国語の領域色。装飾ではなく識別子。一覧の左端3pxの罫として使う */
  --d-speak:      #B23A48;
  --d-write:      #3A5A98;
  --d-read:       #3F7A5E;
  --d-language:   #7A4FA3;
  --d-handwriting:#8A6420;
  --d-reading:    #6B6470;

  --step-0: 1rem; --step-1: 1.125rem; --step-2: 1.375rem; --step-3: 1.75rem;
  --measure: 40em;   /* 日本語で約40字 */

  --page: 880px;
  --radius: 10px;    /* 面（注意書き・空状態） */
  --pill: 999px;     /* チップ・ボタン・入力欄 */
}
```

- 影は使わない。区切りは1pxの罫線か、面の色の差でつける
- 角丸は2種類だけ。押せるものは `--pill`、面は `--radius`。要素ごとに増やさない
- 領域色は色だけで意味を伝えない。必ず文字ラベルを併記する
- 見出しラベル（「校種」「場面」「領域」）は小さく、字間を広げ、主色で出す

### 7.3 タイポグラフィ

**Webフォントを読み込まない。** 校内回線と古い端末を考慮する。

```css
font-family: "Hiragino Kaku Gothic ProN", "Yu Gothic Medium", "Noto Sans JP",
             system-ui, sans-serif;
```

- 本文 17px / 行間 1.8
- 見出しは字面を大きくせず、太さとスペースで差をつける
- 全角英数を使わない。半角英数と日本語の間に手動でスペースを入れない

### 7.4 画面

**トップ（モバイル）**

```
┌──────────────────────────┐
│ 国語のICT活用ヒント        │
│ 有志運営・非公式           │
├──────────────────────────┤
│ 🔍 やりたいことを入力       │  ← フォーカスは当てない
├──────────────────────────┤
│ [ 小学校 ]  [ 中学校 ]     │
├──────────────────────────┤
│ 場面から探す                │
│ [導入][個別][共有][話し合い] │
│ [振り返り][評価][家庭]      │
├──────────────────────────┤
│ 領域から探す                │
│ [話す・聞く][書く][読む]    │
│ [言葉の特徴][書写][読書]    │
├──────────────────────────┤
│ 今週の追加  6件             │
│ ┃ 音声入力でスピーチを…     │  ← 左3px罫が領域色
│   文部科学省 / 準備★☆☆      │
│ …                           │
├──────────────────────────┤
│ 最終更新 2026-09-07         │
└──────────────────────────┘
```

**PC**：中央1カラム、最大880px。絞り込み列を左に出すのは一覧ページのみ。
トップを3カラムに広げない。

**一覧項目**：カードではなく行。左3pxの領域色罫・タイトル・領域・準備の重さ・出典。
区切り文字（`/` や中黒）は置かず、余白で分ける。更新日は行に出さず、詳細と `/updates/` に置く。
1画面に7〜10件入る密度にする（PC。スマホは5件前後）。

**事例詳細**：メタ情報 → 自作要約 → **出典を開くボタン** → 役に立ったボタン → 関連事例3件。
詳細ページで完結させない。出典に送り出すのが仕事。
`gov-open` の事例で本文を引用した場合は、末尾に出典表記を出す。

> 出典：「StuDX Style」（文部科学省）（URL）（2026年9月5日に利用）を加工して作成

### 7.5 やらないこと

カード一律のグリッド／影付きカード／グラデーション／全角大文字ラベル／
`→` 付きのボタン文言／区切り文字でつないだメタ情報／スクロール連動アニメーション／
ヒーロー画像・イラスト素材／飾りだけの絵文字・アイコン

言葉を減らすことを優先する。見出しは名詞1語で足りるなら1語にする
（「校種から探す」ではなく「校種」）。説明文を足す前に、消せる語がないかを先に見る。

---

## 8. 品質基準

- キーボードだけで検索・絞り込み・詳細閲覧ができる。フォーカスリングを消さない
- コントラスト比 4.5:1 以上
- 見出しレベルを飛ばさない。ランドマークを使う
- `prefers-reduced-motion` を尊重
- JSが無効でも一覧と詳細は読める
- Lighthouse: Performance / Accessibility とも 95 以上
- 初回表示の総転送量 200KB 以下（検索インデックス除く）
- 375px 幅で横スクロールが出ない

---

## 9. 受け入れ基準

- [ ] 「話し合いで使えるものを知りたい」中学校の国語教員が、トップから3タップで候補に着く
- [ ] 全ページの本文が、自作の文章か `gov-open` の出典表記付き引用のみで構成されている
- [ ] 外部サイトの画像を1枚も配信していない
- [ ] robots.txt を無視した取得が1件もない（ログで確認できる）
- [ ] `sources.yml` の全ソースに `license` と `license_basis` と `license_checked` がある
- [ ] 推測で書かれたURLが1件もない
- [ ] 中学校国語の事例が20件以上ある
- [ ] 375px 幅と 1440px 幅の両方で §8 を満たす

---

## 10. フェーズ

| | 内容 |
|---|---|
| 1 | Astro雛形、スキーマ、トップ・一覧・詳細、`content-japanese-seed.md` の10件を投入、デプロイ。収集は未実装 |
| 2 | Pagefind検索、scenes / domains / tools / school の絞り込み、`/updates/` |
| 3 | `license-check.ts` と `discover.ts`。まず都道府県10件で試し、`sources.yml` を育てる |
| 4 | `collect.ts` と週次ワークフロー、linkcheck。seed 10件の要約を作り直す |
| 5 | 「役に立った」ボタン、Cookieレスのアクセス解析、学習指導要領コードの付与、他教科への拡張 |

フェーズ1を作ったら、国語の教員数名に見せて構成を見直す。フェーズ3以降を先に作らない。
**フェーズ3は都道府県10件で一度止めて、集まった事例の質を確認してから全国に広げる。**

---

## 11. 未決事項（実装前に確認が必要）

**解決済み**：校務PCから GitHub Pages に到達できることを確認した（2026-09-06）。
公開先は https://japanese-ict-hints.github.io/kokugo-ict-hints/ 。

- 各教育センターのライセンス表記は団体ごとに異なる。**未確認**。§2.1 で1件ずつ判定する
- 教育センターの事例はPDFが多い見込み。PDFからのテキスト抽出が必要（**未検証**）
- 独自ドメインを取るか、`github.io` のままか
- 「役に立った」の集計方法（Microsoft Forms で足りるか）
- 全国から集めると佐倉市で使えないツールの事例も入る。除外するか、注記して載せるか
